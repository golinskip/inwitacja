{% extends 'AppBundle::base.html.twig' %}

{% block body %}
    <!-- Navigation -->
    {% include 'AppBundle:Homepage:navigation.html.twig' %}

    <!-- Header -->
    {% include 'AppBundle:Homepage:header.html.twig' %}

    <!-- Register section -->
    {% include 'AppBundle:Homepage:register.html.twig' with {'form' : formRegistration} %}

    <!-- About Section -->
    {% include 'AppBundle:Homepage:about.html.twig' %}

    <!-- Contact Section -->
    {% include 'AppBundle:Homepage:contact.html.twig' %}

    <!-- Footer -->
    {% include 'AppBundle:Homepage:footer.html.twig' %}

    <!-- Scroll to Top Button (Only visible on small and extra-small screen sizes) -->
    <div class="scroll-top page-scroll hidden-sm hidden-xs hidden-lg hidden-md">
        <a class="btn btn-primary" href="#page-top">
            <i class="fa fa-chevron-up"></i>
        </a>
    </div>
    
{% endblock %}
    
    {% block javascripts %}
        {% javascripts
        '@AppBundle/Resources/public/js/freelancer.min.js'
        '@AppBundle/Resources/public/js/jqBootstrapValidation.js'
        %}
        <script src="{{ asset_url }}"></script>
        {% endjavascripts %}  
    {% endblock %}
