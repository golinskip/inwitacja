
    <section class="fill" id="contact">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 text-center">
                    <h2>{{'menu.contact'|trans}}</h2>
                    <hr class="star-primary">
                </div>
            </div>
            <div class="row">
                {{ form_start(form, {'action' : path('send_message_action')}) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </section>